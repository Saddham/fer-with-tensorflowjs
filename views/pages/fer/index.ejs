<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Expression detection using pre trained model in TensorFlow.js</title>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
        <link href="/css/style.css" rel="stylesheet" type="text/css" media="all" />
    </head>
    <body>
      <div class="container-fluid" style="width: 800px; margin: 0 auto">
        <h1>Expression detection using pre trained model in TensorFlow.js</h1>

        <p>
            Wait for the model to load before clicking the button to enable the webcam - at which point it will become
            visible to use.
        </p>

        <section id="demos" class="invisible">
            <p>
                When ready click "enable webcam" below and accept access to the webcam when the browser asks (check the top left of your window)
            </p>

            <canvas id="webcamCanvas" width="400" height="400" style="margin: 0; display: none"
                >Canvas not supported</canvas
            >
            <div id="liveView" class="camView">
                <div>
                    <div style="padding-bottom: 10px">
                        <button id="webcamButton" class="btn added">Enable Webcam</button>
                        <button id="webcamStopButton" class="btn removed">Stop Webcam</button>
                        <button id="showPredictionButton" class="btn removed">Show Prediction</button>
                    </div>
                    <div style="padding-bottom: 10px">
                      <h3>Predictions:</h3>
                      <ul id="predictions"></ul>
                  </div>
                </div>
                <div>
                  <video id="webcam" autoplay muted width="400" height="400"></video>
                </div>
            </div>
        </section>
        </div>

        <!-- Import TensorFlow.js library -->
        <script src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs/dist/tf.min.js" type="text/javascript"></script>
        <!-- Load the coco-ssd model to use to recognize things in images -->
        <script src="https://cdn.jsdelivr.net/npm/@tensorflow-models/blazeface"></script>

        <!-- Import the page's JavaScript to do some stuff -->
        <script src="/js/script.js" defer></script>
    </body>
</html>
